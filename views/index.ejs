<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

<meta name="google-site-verification" content="bTRlMgrBbckq_jgRj0Eiw0ds2p2lBt0zADKkc6Ydch0" />

  <title>Free URL Shortener - Shorten Your Links Instantly | URL Shortening Service</title>
  <meta name="description" content="Shorten long URLs with our free URL shortener service. Simplify your links, track their performance, and enhance your link management.">
  <meta name="keywords" content="URL shortener, free URL shortener, link shortener, shorten URLs, short links, URL shortening service, link management">
  <meta name="author" content="Alizaib">
  <link rel="icon" type="image/png" href="/public/link.png">

  <link rel="canonical" href="https://sturl-one.vercel.app/">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=Roboto:wght@400&display=swap" rel="stylesheet">

</head>
<style>
/* General Reset */
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Roboto', sans-serif;
  background-color: #121212;
  color: #e0e0e0;
  line-height: 1.6;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* Header */
header {
  text-align: center;
  padding: 2rem;
}

header h1 {
  font-family: 'Montserrat', sans-serif;
  color: #00e676;
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

header p {
  font-size: 1.2rem;
  color: #9e9e9e;
}

/* Main Layout */
main {
  width: 100%;
  max-width: 800px;
  padding: 1.5rem;
  display: grid;
  gap: 2rem;
}

h2 {
  font-family: 'Montserrat', sans-serif;
  color: #00e676;
  font-size: 1.8rem;
  margin-bottom: 1rem;
}

/* Form Section */
#shorten-form {
  display: flex;
  gap: 0.5rem;
}

#shorten-form input[type="text"] {
  flex-grow: 1;
  padding: 0.8rem;
  border: 1px solid #333;
  border-radius: 5px;
  background-color: #333;
  color: #e0e0e0;
}

#shorten-form button {
  padding: 0.8rem 1.5rem;
  background-color: #00e676;
  border: none;
  color: #121212;
  font-weight: bold;
  cursor: pointer;
  border-radius: 5px;
  transition: background-color 0.3s;
}

#shorten-form button:hover {
  background-color: #00c853;
}

/* Result Display */
#result {
  margin-top: 1rem;
  font-size: 1.1rem;
}

#result a {
  color: #00e676;
  text-decoration: underline;
}

#copyButton {
  background-color: #333;
  color: #e0e0e0;
  padding: 0.5rem 1rem;
  border: 1px solid #00e676;
  border-radius: 5px;
  cursor: pointer;
  margin-top: 0.5rem;
}

#copyButton:hover {
  background-color: #00c853;
}

/* Features, Benefits, and Other Sections */
section {
  padding: 1.5rem;
  border-radius: 10px;
  background-color: #1e1e1e;
}

section p,
section ul {
  color: #b0bec5;
  font-size: 1rem;
}

ul {
  list-style-type: none;
  padding-left: 0;
}

ul li {
  padding: 0.3rem 0;
  display: flex;
  align-items: center;
}

ul li::before {
  content: "•";
  color: #00e676;
  margin-right: 0.5rem;
}

/* FAQ */
dt {
  font-weight: bold;
  color: #00e676;
  margin-top: 1rem;
}

dd {
  margin-left: 1.5rem;
  color: #b0bec5;
}

/* Footer */
footer {
  text-align: center;
  padding: 1.5rem;
  width: 100%;
  border-top: 1px solid #333;
  color: #9e9e9e;
  font-size: 0.9rem;
}

footer nav ul {
  list-style-type: none;
  padding: 0;
  display: flex;
  justify-content: center;
  gap: 1rem;
}

footer nav a {
  color: #00e676;
  text-decoration: none;
}

footer nav a:hover {
  text-decoration: underline;
}

</style>

<body>
  <header>
    <h1>Free URL Shortener</h1>
    <p>Shorten long URLs with our easy-to-use service.</p>
  </header>

  <main>
    <section id="shorten">
      <h2>Shorten Your URL</h2>
      <form id="shorten-form">
        <input type="text" id="longUrl" placeholder="Enter URL to shorten" required>
        <button type="submit">Shorten</button>
      </form>
      <div id="result"></div>
    </section>

    <section id="features">
      <h2>Features</h2>
      <ul>
        <li>Instant URL shortening</li>
        <li>Track link performance</li>
        <li>Free and easy to use</li>
        <li>Customizable short URLs</li>
        <li>Secure and reliable</li>
      </ul>
    </section>

    <section id="how-it-works">
      <h2>How It Works</h2>
      <p>Our URL shortener takes your long URLs and converts them into shorter, more manageable links. Simply paste your long URL into the input box above and click "Shorten" to get started.</p>
      <p>Each shortened URL is unique and can be tracked to analyze performance, providing you with valuable insights into your link's engagement.</p>
    </section>

    <section id="benefits">
      <h2>Benefits of Using Our URL Shortener</h2>
      <ul>
        <li>Improved link aesthetics</li>
        <li>Better click-through rates</li>
        <li>Easy sharing on social media</li>
        <li>Enhanced link management</li>
        <li>Increased brand visibility with custom URLs</li>
      </ul>
    </section>

    <section id="use-cases">
      <h2>Use Cases</h2>
      <p>Our URL shortener can be used in various scenarios:</p>
      <ul>
        <li>Marketing campaigns</li>
        <li>Social media sharing</li>
        <li>Email campaigns</li>
        <li>Affiliate links</li>
        <li>Linking to documents and resources</li>
      </ul>
    </section>

    <section id="faq">
      <h2>Frequently Asked Questions</h2>
      <dl>
        <dt>Is the URL shortener service free?</dt>
        <dd>Yes, our URL shortener is completely free to use.</dd>
        <dt>Can I track the performance of my shortened URLs?</dt>
        <dd>Yes, you can track clicks and other performance metrics for your shortened URLs.</dd>
        <dt>Is it possible to customize my shortened URLs?</dt>
        <dd>Yes, you can create custom short URLs for better branding and recognition.</dd>
        <dt>How secure is the URL shortener?</dt>
        <dd>We use secure protocols to ensure your links are safe and reliable.</dd>
      </dl>
    </section>
  </main>

  <footer>
    <p>&copy; 2024 Alizaib All rights reserved.</p>
    <nav>
      <ul>
        <li><a href="#shorten">Shorten</a></li>
        <li><a href="#features">Features</a></li>
        <li><a href="#how-it-works">How It Works</a></li>
        <li><a href="#benefits">Benefits</a></li>
        <li><a href="#use-cases">Use Cases</a></li>
        <li><a href="#faq">FAQ</a></li>
      </ul>
    </nav>
  </footer>
</body>

<script>
  document.getElementById('shorten-form').addEventListener('submit', async function(event) {
    event.preventDefault();
    const longUrl = document.getElementById('longUrl').value;

    const response = await fetch('/shorten', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({ longUrl })
    });

    const result = await response.json();
    const resultDiv = document.getElementById('result');

    if (result.shortUrl) {
      resultDiv.innerHTML = `
        <p>Short URL: <a href="${result.shortUrl}" id="shortUrl">${result.shortUrl}</a></p>
        <button id="copyButton">Copy to Clipboard</button>
      `;

      document.getElementById('copyButton').addEventListener('click', function() {
        const shortUrlElement = document.getElementById('shortUrl');
        const range = document.createRange();
        range.selectNode(shortUrlElement);
        window.getSelection().removeAllRanges();
        window.getSelection().addRange(range);

        try {
          document.execCommand('copy');
          window.getSelection().removeAllRanges();
          alert('Short URL copied to clipboard');
        } catch (err) {
          alert('Failed to copy text');
        }
      });
    } else {
      resultDiv.innerHTML = `<p>Error: ${result.error}</p>`;
    }
  });
</script>

</html>


